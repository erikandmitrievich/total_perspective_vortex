#+title: Design
#+author: Erik An
#+email: obluda2173@gmail.com
#+date: <2026-08-26>
#+lastmod: <2026-08-27 18:13>
#+options: toc:nil num:nil tags:nil
#+startup: overview inlineimages

* Infrastructure

Deferred until the per-module descriptions are complete.

* Builtins

** argparse

Builds the CLI of =mybci.py=: flags, constraints, and the usage errors raised after the =SUBJECTS= and =runs_for= checks.

** sys

Feeds =sys.argv[1:]= into =main()= and carries its return value out as the process exit status.

** time

Brackets each per-chunk prediction in =score_stream=, which is what makes the <2 s latency claim measurable rather than asserted.

** dataclasses

Parameter records, so defaults live in one declaration instead of scattered call sites.

- =dataclass=: Declares =PreprocConfig= and =FitConfig=, both of which carry values only, no behaviour.

** pathlib

Every filesystem location in the project is a =Path=, never a string.

- =Path=: Builds =MODELS_DIR= and the per-group model file in =model_path=. Also the =path= threaded into =load_raw= and the existence check in =predict=.

* Externals

** mne

The EEG layer. Everything in [[file:../src/data.py][data.py]] that is not arithmetic comes from here.

- =Epochs=: Cuts =filtered= into the =tmin=/=tmax= window around each event in =make_epochs=, and is the object =load_dataset= hands back for figures.
- =pick_types=: Restricts to EEG channels before epoching, dropping everything the montage does not describe.
- =channels.make_standard_montage=: Attaches the electrode positions named by =PreprocConfig.montage=; without it =patterns_= cannot be plotted as a topomap.
- =datasets.eegbci=: Resolves subject and run numbers to local EDF paths, fetching on first use, and standardises channel names.
- =io.read_raw_edf=: Reads one run file into a =Raw=.
- =io.concatenate_raws=: Joins the three runs of a group into a single =Raw=. Legitimate only because T1 and T2 keep one meaning across the group (see =BASE_GROUPS=).

** numpy

Array backend throughout; the covariance reshape and the pseudo-inverse in [[file:../src/csp.py][csp.py]] are plain numpy.

- =typing=: =NDArray= annotations on the arrays that cross module boundaries — =X=, =y=, features.

** scipy

Used for exactly one call, but that call is the algorithm.

- =linalg.eigh=: Solves the generalised problem =eigh(S_0, S_0 + S_1)= in =MyCSP.fit=, returning the joint diagonaliser as eigenvectors.

** sklearn

Supplies the estimator contract =MyCSP= implements, the classifier behind it, and the splitting machinery.

- =base.BaseEstimator=: Gives =MyCSP= =get_params=/=set_params=; cross-validation clones estimators through these, so without it =cross_val_score= cannot re-instantiate the step.
- =base.TransformerMixin=: Supplies =fit_transform=, which is what =Pipeline= calls on intermediate steps.
- =discriminant_analysis.LinearDiscriminantAnalysis=: Terminal step of the pipeline. Being linear is precisely why the log transform in =MyCSP.transform= earns its place.
- =model_selection.StratifiedShuffleSplit=: The =cv= object in =fit_pipeline= — =n_splits= draws at =test_size=, class ratio preserved in each.
- =model_selection.cross_val_score=: Produces =scores=, the per-fold accuracies reported by =train=.
- =model_selection.train_test_split=: Splits =idx= into =train_idx= and =test_idx= under =stratify=; =test_idx= is the partition =predict= later replays.
- =pipeline.Pipeline=: Binds =MyCSP= to LDA so the spatial filters are fit on training folds only. This is the whole leakage guarantee.
- =utils.validation.check_is_fitted=: Guards =MyCSP.transform= against being called before =fit=.

** joblib

Serialises the fitted pipeline and its provenance to =model_path(...)= in =train=, and reads it back in =predict=. The only disk round trip in the project, and the reason =sweep= bypasses both.

** matplotlib

Imported by [[file:../visualise.py][visualise.py]] only; nothing on the training path touches it.

- =pyplot=: Draws the before/after PSD panels, fed by =load_raw= (unfiltered) and =preprocess= (filtered).

* [[file:../src/experiments.py][experiments.py]]

Defines run groups. Imported by [[file:../src/sweep.py][sweep.py]], [[file:../mybci.py][mybci.py]] and [[file:../visualise.py][visualise.py]].

** =BASE_GROUPS=

The four task groups, as =(runs, label)= pairs (see [[file:eegmmidb.org][eegmmidb.org]]). Each group is one task repeated three times, so T1 and T2 keep one meaning across all three runs, which is what allows concatenation. Labels are printed by the sweep.

** =EXPERIMENTS=

Defines experiments for =run_full_sweep()=. Just adds additional two experiments on top of base groups, each pairing an executed group with its imagined counterpart.

** =SUBJECTS=

Merely a range of subjects. Exists for coherence: both CLIs validate against it, =run_full_sweep()= iterates it.

** =runs_for(run)=

Expands =run= to =runs=: =3= -> =[3, 7, 11]=. One run is not a binary problem on its own, the group is the training unit. Called by [[file:../mybci.py][mybci.py]] (=train=, =predict=) and [[file:../visualise.py][visualise.py]].

* [[file:../src/data.py][data.py]]

Where the data comes from.

** =PreprocConfig=

Default preprocessing parameters.

- =l_freq=: lower bound useful frequency
- =h_freq=: upper bound useful frequency
- =tmin=: start of the feature window, 1 s post-onset
- =tmax=: end of the feature window, 2 s post-onset
- =montage=: standard electrode layout, attached for channel positions
- =fir_design=: FIR design forwarded to =Raw.filter=

** =DEFAULT=

Just a default instance of =PreprocConfig=.

** =EVENT_ID=

Events code, pinned: T1 -> 1, T2 -> 2.

** =load_raw(subject, runs, *, path)=

Gets raw file names of subject's runs (=raw_fnames=). Reads and concatenates raw data from =raw_fnames= to =raw=. Returns =raw=. No filtering, so it also serves the "before" panel of the PSD figure.

** =preprocess(raw, config)=

Makes a copy of a =raw=, calls it =filtered=. Standardizes, sets a montage, and applies a filter. Returns =filtered=.

** =make_epochs(filtered, config)=

Selects solely eeg from filtered, calls it =picks=. Gets the =events= via =events_from_annotations= with the pinned =EVENT_ID=. Creates epochs. Returns them.

** =_labels_from_events(epochs)=

Just returns shifted event labels to comply with project notation.

** =load_dataset(subject, runs, config, *, path)=

Gets =raw=. Preprocesses and epochs it. Returns =X= (epochs as array), =y= (labels) and epochs (epochs as object).

* [[file:../src/csp.py][csp.py]]

Heart of project. A from scratch implementation of CSP dimensionality reduction algorithm.

** =MyCSP(BaseEstimator, TransformerMixin)=

A class, inherits from =BaseEstimator= and =TransformerMixin=.

*** =__init__(n_components, log)=

Initialises self attributes from given arguments.

*** =_covariance(X)=

Gets the number of channels (=n_channels=). Reshapes the 3D tensor from =(epochs, channels, time)= to a 2D matrix of shape =(channels, epochs * time)= through tensor transposition and flattening. This concatenates the time-series of all epochs. Calculates covariance matrix (see "Concatenation estimator" from [[file:csp.org][csp.org]]).

*** =fit(X, y)=

Validates =n_components= is even and =classes= count being exactly 2. Assigns =self.classes_=, and calculates covariance matrices for different classes (=S_0=, =S_1=). Calls =eigh(S_0, S_0 + S_1)= and obtains eigenvalues and eigenvectors: =eigvals=, =eigvecs= (see "Why =eigh= is the right solver" from [[file:csp.org][csp.org]]). Stores descendingly ordered eigenvalues and eigenvectors in =self.eigvals_= and =self.filters_= (=self.filters_= holds the eigenvectors as rows; see "Convention warning" from [[file:csp.org][csp.org]]). In parallel, stores pseudo-inverse of spatial filters in =self.patterns_= — what a topomap should display; nothing consumes it yet.

Summing up, it calculates filter matrix which will be used to reduce dimensionality of raw epoch data. It's the main function of CSP.

*** =transform(X)=

Verifies the model is fitted using =check_is_fitted(self)= (which ensures the required learned attributes exist). Selects the leftmost and rightmost filters, as they produce the most discriminative variance between the two classes. Projects the raw data onto these selected filters (resulting in =X_prj=). Calculates the =features= array of shape =(n_epochs, n_components)=, where each value essentially represents the energy (variance) of the filtered brain activity for one component of one epoch. Finally, if =self.log= is set to =True=, a log transformation is applied to the features to compress extreme spikes and create a normal (bell-curve) distribution, which is much easier for linear classifiers to process. Returns features.

** TODO why inherits?

* [[file:../src/train.py][train.py]]

Where training happens.

** =FitConfig=

Stores default split and estimator parameters used for training and sweep.

- =n_components= is responsible for the number of filters to be applied.
- =test_size= for the holdout fraction and the validation fraction inside each cv split.
- =n_splits= for number of validation tests.
- =seed= is the seed used for reproducibility.

** =model_path(subject, runs)=

Uses global =MODELS_DIR= to construct a path to a fitted model. Used in writing and reading fitted models by =train(...)= and =predict(...)=.

** =make_pipeline(n_components)=

Just returns the =Pipeline= object. Exists for convenience.

** =fit_pipeline(X, y, fit, *, cross_validate)=

=idx= represent array of indices (of labels). =train_test_split(...)= performs split of indices based on given arguments into train and test.

- =test_size= for training and test ratio
- =stratify= for keeping the original class ratio
- =random_state= for random state

Then makes a pipeline. If =cross_validate= is set, makes a cross-validation object (=cv=) and stores accuracy of each fold in =scores=; otherwise =scores= is =None=. And finally, fits the model with =fit(...)= and returns a classifier (=clf=), train indices (=train_idx=), sorted test indices (=test_idx=), and scores.

** =train(subject, runs, config, fit, *, verbose)=

Performs loading of preprocessed epochs and labels (=X=, =y=). Gets classifier, train and test indices, and scores from =fit_pipeline(...)=. Creates a model directory just in case and populates created model (through =joblib.dump(...)=) with the payload; =predict= reads only =pipeline=, =config=, =test_idx= and =len(train_idx)=, the rest is provenance. Then goes =if verbose= condition, necessary for sweep vs train and predict. Returns =scores.mean()=

* [[file:../src/predict.py][predict.py]]

Where a persisted model meets its held-out partition.

** =score_stream(clf, X, y, test_idx, *, verbose)=

Enters a for loop of test indices. Measures start time, stores predicted class for =X[i]= to =preds=, measures end time, and stores time delta in =latencies= per chunk (implemented to comply subject's requirements of <2s latency per chunk). Prepares and compares predictions versus truths, calculates the mean. =if verbose= (for =predict= mode) prints subject's outlined CLI table. Returns accuracy.

** =predict(subject, runs, *, verbose)=

Gets a model path from =subject= and =runs= (and checks path existence). Loads the model to =d=. Loads =X=, =y= from dataset (reloads unders persisted from model config). Checks model loaded indices length matches dataset loaded truth length (prevents different epochs comparison). Runs =score_stream= and returns accuracy.

* [[file:../src/sweep.py][sweep.py]]

Brings everything together and outputs — this is what discharges the 60% requirement. Unlike =train= -> =predict=, goes through =fit_pipeline= -> =score_stream=, dodging the disk round trip.

** =evaluate(subject, runs, config, fit)=

Loads epochs and labels under given config. Fits pipeline with =cross_validate=False= (the sweep reports held-out accuracy only) and runs score stream with =verbose=False= (without saving a model). Gets and returns =acc=.

** =run_full_sweep()=

Goes through each experiment, always on defaults. For each experiment calculates accuracy of each subject, prints it out. Calculates the mean accuracy per experiment, prints it as well. Calculates mean accuracy across experiments, prints it as well.

Each cell is wrapped in =try/except=, so one bad subject cannot kill the run; failures are printed at the end and the experiment mean is taken over successful cells only, hence the =n== in the output. Returns the per-experiment means as a list.

* [[file:../mybci.py][mybci.py]]

An entrypoint of the project.

** =parse_args(argv)=

Is called from =main()=. Main purpose is to parse the given =argv=.

Creates an object =parser= (from module =argparse=). Defines arguments with validation and constraints (relative to subject, run, and mode). Parses according to outlined parser configuration (=parser.parse_args(argv)=). Goes through another validation step: subject against =SUBJECTS=, and run by membership in a =BASE_GROUPS= entry — delegated to =runs_for=, whose =ValueError= is surfaced as a usage error. Stores the resolved group in =args.runs=. Returns parsed arguments.

** =main(argv)=

Calls =parse_args(argv)=. Parsed arguments branch program to 3 paths:

- =args.subject == None=

  Calls =run_full_sweep()=.

- =args.mode == "train"=

  Calls =train(args.subject, args.runs)=.

- =args.mode == "predict"=

  Calls =predict(args.subject, args.runs)=.
